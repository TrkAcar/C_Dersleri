#include <stdio.h>
#include <stdlib.h>

int	main()
{
	int kizarkadas;
	kizarkadas = 5;

	do
	{
		printf("%d Adet kız arkadaşın kaldı\n", kizarkadas);
		kizarkadas = kizarkadas - 1;
	}
	while (kizarkadas > 0);
		printf("Sap kaldın garDeŞş");
	return (0);
}
